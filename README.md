# forest-sorting
Sorting a forest of nodes deterministically

## Building

This repository uses CMake and a C++20 compiler.

```bash
mkdir -p build
cd build
cmake ..
cmake --build .
./forest-sorting
```
